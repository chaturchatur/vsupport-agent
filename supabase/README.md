# Supabase Configuration

## Migrations (applied in order)

### `001_initial_schema.sql` — Core Tables

Creates the two main tables, migrated from Airtable to eliminate its 5 req/sec rate limit:

**`customers`** (read-only by the voice agent)
- `id` — auto-incrementing bigint primary key
- `first_name`, `last_name`, `phone_number` (E.164 format like `+14155551234`)
- `claim_number` — **UNIQUE** constraint, the primary business identifier
- `claim_status` — constrained to `approved`, `pending`, `requires_documentation` via CHECK
- `claim_details` — free text (e.g. "Missing proof of loss form...")
- **3 indexes** matching the voice agent's lookup patterns: phone number, `LOWER(last_name)`, `UPPER(claim_number)`

**`interactions`** (write log for call events)
- `caller_name`, `phone_number`, `summary`, `sentiment` (CHECK: positive/neutral/negative)
- `vapi_call_id` — **UNIQUE** constraint. This is the key architectural fix: enables `INSERT ... ON CONFLICT DO NOTHING` for atomic dedup. If n8n's `log_interaction` tool and the end-of-call-report both fire for the same call, the second insert silently no-ops instead of creating a duplicate.
- Indexed on `vapi_call_id` (for dedup lookups) and `timestamp DESC` (for recent-first queries)

---

### `002_phone_digits_column.sql` — PostgREST Encoding Workaround

Adds a **generated column** `phone_digits` that strips the `+` from `phone_number`:

```sql
phone_digits TEXT GENERATED ALWAYS AS (replace(phone_number, '+', '')) STORED
```

**Why**: The Supabase n8n node runs `encodeURI()` on filter strings. In URL encoding, `+` means space, so filtering by `+14155551234` becomes `14155551234` (broken). By querying the `phone_digits` column (digits only, e.g. `14155551234`), n8n can match correctly without encoding issues. The canonical E.164 `phone_number` column is preserved untouched.

---

### `003_add_date_of_birth.sql` — Identity Verification

Adds `date_of_birth DATE` to `customers`. The voice agent asks callers for their DOB after phone lookup and compares it against this field before revealing any claim information. This is a basic identity verification step.

---

### `004_enable_pgvector.sql` — Vector Extension

```sql
CREATE EXTENSION IF NOT EXISTS vector WITH SCHEMA extensions;
```

Enables the `pgvector` extension (under the `extensions` schema, Supabase convention) for storing and querying vector embeddings. Required by migration 005.

---

### `005_faqs_table.sql` — FAQ Semantic Search

Creates the **`faqs`** table:
- `question`, `answer`, `category` — the FAQ content
- `embedding extensions.vector(384)` — a 384-dimensional vector column (matches `gte-small` model output dimension)

Also creates the **`match_faqs`** RPC function:
- Takes a `query_embedding` vector and a `match_threshold` (default 0.8)
- Uses the **inner product operator** `<#>` (negative inner product) — since gte-small embeddings are L2-normalized, inner product is equivalent to cosine similarity but faster
- The `WHERE` clause uses `< -match_threshold` because `<#>` returns the *negative* inner product (so a threshold of 0.8 becomes -0.8)
- Returns matching FAQ rows ordered by similarity

---

## Seed Files

### `seed.sql` — 5 Sample Customers

Inserts 5 test customers (Sarah Johnson, Michael Johnson, Emily Chen, David Martinez, Lisa Park) with different claim statuses. Two Johnsons exist for testing multi-match disambiguation. Emily Chen has `claim_details` populated. Uses `ON CONFLICT (claim_number) DO NOTHING` for idempotent re-runs.

### `seed_faqs.sql` — 18 Sample FAQs

Inserts 18 FAQ entries covering categories: `general`, `claims`, `billing`. Topics include office hours, claims process, deductibles, discounts, policy cancellation, etc. **No embeddings included** — they're auto-generated by the `generate-embedding` Edge Function via a database webhook on INSERT.

---

## Edge Functions

### What Are Edge Functions?

Supabase Edge Functions are **serverless TypeScript functions** that run on **Deno** (not Node.js) at the edge, deployed to Supabase's infrastructure. Think of them like AWS Lambda or Cloudflare Workers but tightly integrated with Supabase — they automatically get access to `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` env vars, and can use `Supabase.ai` for built-in ML inference without any external API calls.

Key characteristics:
- **Deno runtime** — imports use URL-based ESM (`https://esm.sh/...`), not `npm install`
- **No cold start billing** — you pay per invocation, not per uptime
- **Built-in Supabase AI** — `new Supabase.ai.Session("gte-small")` runs embedding models directly on Supabase infra, no OpenAI key needed
- **Invoked via HTTP** — each function gets a public URL like `https://<project-ref>.supabase.co/functions/v1/<function-name>`

### Trigger Patterns

There are two trigger patterns used in this project:

1. **Database Webhook** — Configured in the Supabase dashboard to fire on table events (INSERT, UPDATE, DELETE). Supabase POSTs the `{ record }` payload to the function URL automatically.
2. **External HTTP call** — Any service (VAPI, n8n, your app) calls the function URL directly as a regular HTTP endpoint.

### Why Edge Functions Instead of FastAPI?

The FastAPI server in this project is a thin utility layer (`/health`, `/normalize-phone`). The edge functions handle the AI-heavy work because:

- **`Supabase.ai.Session`** runs gte-small on Supabase's own infra — zero latency to the database, no external embedding API needed
- **Direct database access** — the function and Postgres are in the same Supabase project, so RPC calls are fast
- **No server to manage** — they scale automatically and you don't need to keep a process running

### `functions/generate-embedding/index.ts` — Auto-Embedding Webhook

Triggered by a **Supabase Database Webhook** on `faqs` table INSERT/UPDATE events:

1. Receives the `record` payload from the webhook
2. Combines `question + answer` into a single input string
3. Runs it through `Supabase.ai.Session("gte-small")` — Supabase's built-in inference API for the `gte-small` embedding model (384 dims, runs on Supabase infrastructure)
4. Uses `mean_pool: true` and `normalize: true` (L2 normalization, which is why `<#>` works as cosine similarity)
5. Updates the row's `embedding` column with the result

This means you never manually compute embeddings — just INSERT a question/answer and the webhook fills in the vector automatically.

### `functions/search-faq/index.ts` — VAPI Knowledge Base Endpoint

This is the **custom knowledge base** for VAPI. When the voice agent needs FAQ answers mid-call:

1. VAPI sends a `knowledge-base-request` webhook to this function
2. **HMAC SHA-256 verification** — validates `x-vapi-signature` header against `VAPI_SECRET` env var
3. Extracts the latest user message from the conversation history
4. Embeds the query using the same `gte-small` model
5. Calls the `match_faqs` RPC with threshold 0.8, limited to top 3 results
6. Returns results in **VAPI's expected format**: `{ documents: [{ content, similarity, uuid }] }`

The `content` field is formatted as `Q: ... \nA: ...` so the voice agent's LLM can naturally incorporate the answer into its response.

---

## Data Flow Summary

```
FAQ Insert → DB Webhook → generate-embedding → writes vector back to row
                                                        ↓
Caller asks question → VAPI → search-faq → embeds query → match_faqs RPC → top 3 FAQs → VAPI reads answer to caller
```

```
Caller phones in → VAPI → n8n → queries customers (phone_digits / last_name / claim_number)
                                → verifies DOB
                                → returns claim info
                                → logs to interactions (dedup via vapi_call_id UNIQUE)
```
